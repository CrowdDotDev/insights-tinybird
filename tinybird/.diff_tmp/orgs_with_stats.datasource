# Data Source created from Pipe 'orgs_with_stats_mv'

SCHEMA >
    `o.ORG_NAME` Nullable(String),
    `commit_count` UInt64,
    `pr_count` UInt64,
    `lines_of_code` Int64

ENGINE "MergeTree"
ENGINE_SORTING_KEY "commit_count, pr_count, lines_of_code"
